---
- name: Install Ruby and Bundler
  hosts: all
  become: true

  tasks:
    - name: Install Ruby
      apt:
        name: ['ruby-full','ruby-bundler','build-essential']
        state: present
        update_cache: yes
